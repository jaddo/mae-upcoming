# Code owners for MAE Upcoming.
#
# Ownership record and operations runbook: ../HANDOVER.md
#
# Jeff Addo (@jaddo) is the incoming owner. He already has write access, which
# is what CODEOWNERS requires, so these rules route now rather than only after
# the transfer completes.

* @jaddo

# The ORFE -> MAE inversion is the standing trap in this repo: MAE and ORFE put
# the same information in opposite fields, both validate against the same
# schema, and a swapped mapping produces schema-valid nonsense rather than an
# error. These four files are where that gets decided, so they want the owner's
# eyes even if review is relaxed elsewhere.

/transform_config.json           @jaddo
/tests/test_mae_shape.py         @jaddo
/tests/test_pipeline_contract.py @jaddo
/HANDOVER.md                     @jaddo
